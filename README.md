# 🧠 EfficientNetB0 Image Classification

This repository contains an implementation of an image classification model using **EfficientNetB0**, a state-of-the-art convolutional neural network architecture designed for efficient scaling of depth, width, and resolution.

---

## 📁 Project Overview

This project demonstrates how to use **TensorFlow** and **Keras** to train and evaluate an image classifier using the EfficientNetB0 architecture.  
It includes data preprocessing, model training, evaluation, and visualization of metrics.

---

## ⚙️ Features

- Uses **EfficientNetB0** for feature extraction and classification  
- Supports **custom image datasets**  
- Includes **training and validation accuracy/loss visualization**  
- Easily extendable for transfer learning and fine-tuning  
- GPU-compatible for faster training  

---

## 🧩 Requirements

Install the required dependencies before running the notebook:

```bash
pip install tensorflow numpy matplotlib scikit-learn
